on:
  push:
    tags:
      - 'v*' # Push events to matching v*, i.e. v1.0, v20.15.10

name: Upload Release Asset

jobs:
  build:
    name: Upload Release Asset
    runs-on: windows-2019
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          submodules: 'recursive'
      - name: Setup
        run: |
          pip install nuitka
          pip install -r requirements.txt
      - name: Build
        run: |
          C:
          cd "Program Files (x86)"
          dir
          cd "Microsoft Visual Studio"
          dir
          cd "2019"
          dir
          cd "Enterprise"
          dir
          cd "VC"
          dir
          cd "Auxiliary"
          dir
          cd "Build"
          dir
          vcvarsall.bat x64
          D:
          set
          python -m nuitka --standalone --python-flag=-S --windows-dependency-tool=pefile race-info.py
        shell: cmd
      - name: Compress
        run: |
          Compress-Archive race-info.dist race-info.zip
        shell: powershell
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          draft: false
          prerelease: false
      - name: Upload Release Asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./race-info.zip
          asset_name: race-info.zip
          asset_content_type: application/zip
